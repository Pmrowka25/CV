# Konfiguracja połączenia z bazą danych PostgreSQL

DB_HOST=localhost
DB_PORT=5432
DB_NAME=opensky
DB_USER=postgres
DB_PASSWORD=...

# Interwał importu w trybie --loop (w sekundach)
# Domyślnie: 120 (2 minuty)

IMPORT_INTERVAL_SEC=120
